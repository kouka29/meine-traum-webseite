import TradePlaceholder from "./TradePlaceholder";

const HandwerkerKontakt = () => (
  <TradePlaceholder title="Kontakt für Handwerker" subtitle="Schreib uns – wir melden uns innerhalb von 2 Stunden." />
);

export default HandwerkerKontakt;
