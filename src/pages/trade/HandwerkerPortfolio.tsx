import TradePlaceholder from "./TradePlaceholder";

const HandwerkerPortfolio = () => (
  <TradePlaceholder title="Portfolio Handwerker" subtitle="Beispiele unserer Handwerker-Websites." />
);

export default HandwerkerPortfolio;
