import TradePlaceholder from "./TradePlaceholder";

const HandwerkerPreise = () => (
  <TradePlaceholder title="Preise für Handwerker-Websites" subtitle="Transparente Pakete für Handwerksbetriebe." />
);

export default HandwerkerPreise;
